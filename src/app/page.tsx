import Hero from "@/components/landing/Hero";

/**
 * Home page component
 * Displays the landing page with hero section
 *
 * @returns {JSX.Element} The landing page
 */
export default function Home() {
  return <Hero />;
}
